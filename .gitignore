# Python
__pycache__/
*.py[cod]
*.egg-info/

# MacOS
.DS_Store

# SLURM logs
slurm-logs/
*.out

# Large / external data (kept outside the repo)
*.hdf5

# Machine-local paths. Copy the .template files and edit; never commit these.
paths.yaml
paths.sh

# PTB-derived text. LDC-licensed and not ours to redistribute, but useful to
# keep locally: sentences.txt is the extractors' input, and reporter.py writes
# the .tikz dependency renderings on every probe run.
experiments/*/data/sentences.txt
*.tikz
