dataset:
  observation_fieldnames:
     - index
     - sentence
     - lemma_sentence
     - upos_sentence
     - xpos_sentence
     - morph
     - head_indices
     - governance_relations
     - secondary_relations
     - extra_info
     - embeddings
  corpus:
    root: PLACEHOLDER  # set from paths.sh at submit time
    train_path: ptb3-wsj-train.conllx
    dev_path: ptb3-wsj-dev.conllx
    test_path: ptb3-wsj-test.conllx
  embeddings:
    type: subword
    root: PLACEHOLDER  # set from paths.sh at submit time
    train_path: raw.train.bertbase-natural-layers.hdf5
    dev_path: raw.dev.bertbase-natural-layers.hdf5
    test_path: raw.test.bertbase-natural-layers.hdf5
  batch_size: 20
model:
  hidden_dim: 768
  model_type: BERT-disk
  use_disk: True
  model_layer: 3  # overridden per-layer by run_all_hface_prealigned.sh
  alignment: pre-aligned
  alignment_root: PLACEHOLDER  # set from paths.sh at submit time
  alignment_train_path: alignments.train.hdf5
  alignment_dev_path: alignments.dev.hdf5
  alignment_test_path: alignments.test.hdf5
probe:
  task_signature: word_pair
  task_name: parse-distance
  maximum_rank: 64
  psd_parameters: True
  diagonal: False
  params_path: predictor.params
probe_training:
  epochs: 40
  loss: L1
  report_uuas_during_training: True
reporting:
  root: experiments/bert-base-prd/results-hface  # overridden by --results-dir
  observation_paths:
    train_path: train.observations
    dev_path: dev.observations
    test_path: test.observations
  prediction_paths:
    train_path: train.predictions
    dev_path: dev.predictions
    test_path: test.predictions
  reporting_methods:
    - spearmanr
    - uuas
