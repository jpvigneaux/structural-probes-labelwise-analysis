# Core probe dependencies (original)
Cython==3.2.4
seaborn==0.13.2
scipy==1.17.0
PyYAML==6.0.2
numpy==2.4.2
h5py==3.15.1
tqdm==4.67.3

# Deep learning
torch==2.10.0               # latest cu12 build; 2.11.0+ switched to cu13 (incompatible with cluster driver)
transformers>=4.56          # v5 breaking changes (wordpiece_tokenizer, add_special_tokens kwarg) fixed in codebase
huggingface_hub==0.36.2
transformer-lens>=2.0.0     # mechanistic interpretability hooks (HookedEncoder for BERT)

# Analysis and visualisation (added for experiments/)
matplotlib==3.10.8
pandas==3.0.0
plotly==6.6.0

# SLURM job submission
simple_slurm==0.3.6

# Character-level alignment for hface tokenisation matching (hface_alignment in data.py)
python-Levenshtein==0.27.3

# Fairseq checkpoint loading (roberta-shufflen1-prd)
omegaconf==2.3.0
